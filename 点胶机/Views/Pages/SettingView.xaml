<UserControl x:Class="点胶机.Views.Pages.SettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:点胶机.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:SettingViewModel}"
             mc:Ignorable="d">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Title}" FontSize="22" FontWeight="Bold" Margin="0,0,0,12" />

        <StackPanel Grid.Row="1">
            <!-- 工作模式 -->
            <Border Padding="16" CornerRadius="6" Background="{DynamicResource MahApps.Brushes.Control.Background}" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="工作模式" FontSize="14" FontWeight="Bold" Margin="0,0,0,8" />
                    <StackPanel Orientation="Horizontal">
                        <ComboBox SelectedIndex="{Binding WorkModeIndex}" Width="120">
                            <ComboBoxItem Content="手动" />
                            <ComboBoxItem Content="自动" />
                            <ComboBoxItem Content="空跑" />
                        </ComboBox>
                    </StackPanel>
                    <TextBlock Text="提示:切换工作模式立即生效。空跑模式跳过到位等待,用于纯逻辑验证。"
                               FontSize="11" Opacity="0.6" Margin="0,8,0,0" />
                </StackPanel>
            </Border>

            <!-- 报警演示 -->
            <Border Padding="16" CornerRadius="6" Background="{DynamicResource MahApps.Brushes.Control.Background}" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="报警演示(仿真)" FontSize="14" FontWeight="Bold" Margin="0,0,0,8" />
                    <StackPanel Orientation="Horizontal">
                        <Button Content="触发急停" Padding="14,6" Command="{Binding TriggerEstopCommand}" Background="Red" Margin="0,0,8,0" />
                        <Button Content="触发气压故障" Padding="14,6" Command="{Binding TriggerPressureFaultCommand}" Margin="0,0,8,0" />
                        <Button Content="清除气压故障" Padding="14,6" Command="{Binding ClearPressureFaultCommand}" />
                    </StackPanel>
                    <TextBlock Text="用于验证报警系统:弹窗、报警页、三色灯、蜂鸣、MySQL 落库。"
                               FontSize="11" Opacity="0.6" Margin="0,8,0,0" />
                </StackPanel>
            </Border>

            <!-- 数据库信息 -->
            <Border Padding="16" CornerRadius="6" Background="{DynamicResource MahApps.Brushes.Control.Background}">
                <StackPanel>
                    <TextBlock Text="数据库" FontSize="14" FontWeight="Bold" Margin="0,0,0,8" />
                    <TextBlock Text="{Binding DbStatus}" FontSize="11" Opacity="0.7" TextWrapping="Wrap" />
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
