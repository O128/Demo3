<UserControl x:Class="点胶机.Views.Pages.AlarmView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:点胶机.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:AlarmViewModel}"
             mc:Ignorable="d">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Title}" FontSize="22" FontWeight="Bold" Margin="0,0,0,12" />

        <!-- 当前报警 -->
        <Border Grid.Row="1" Padding="12" CornerRadius="6" Background="{DynamicResource MahApps.Brushes.Control.Background}" Margin="0,0,0,12">
            <StackPanel>
                <DockPanel LastChildFill="False">
                    <TextBlock Text="当前报警" FontSize="14" FontWeight="Bold" DockPanel.Dock="Left" />
                    <Button Content="确认报警(Ack)" DockPanel.Dock="Right" Command="{Binding AckCommand}" Padding="14,4" />
                </DockPanel>
                <ItemsControl ItemsSource="{Binding ActiveAlarms}" Margin="0,8,0,0">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="Red" BorderThickness="1" CornerRadius="3" Padding="8,4" Margin="0,2">
                                <TextBlock>
                                    <Run Text="#" /><Run Text="{Binding AlarmId}" />
                                    <Run Text=" [" /><Run Text="{Binding Level}" /><Run Text="] " />
                                    <Run Text="{Binding Name}" />
                                    <Run Text="  " /><Run Text="{Binding StartTime, StringFormat=HH:mm:ss}" />
                                </TextBlock>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <TextBlock Text="(无活跃报警)" Foreground="Gray" Margin="0,4" >
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ActiveAlarms.Count}" Value="0">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- 历史报警筛选 -->
        <DockPanel Grid.Row="2" LastChildFill="False" Margin="0,0,0,8">
            <TextBlock Text="历史报警(最近 " VerticalAlignment="Center" />
            <ComboBox Width="80" SelectedIndex="0" VerticalAlignment="Center"
                      SelectedValuePath="Tag" SelectionChanged="OnFilterChanged">
                <ComboBoxItem Content="1 天" Tag="1" />
                <ComboBoxItem Content="7 天" Tag="7" />
                <ComboBoxItem Content="30 天" Tag="30" />
            </ComboBox>
            <TextBlock Text=" 天)" VerticalAlignment="Center" />
            <Button Content="刷新" Command="{Binding LoadHistoryCommand}" Padding="14,4" DockPanel.Dock="Right" />
        </DockPanel>

        <!-- 历史报警表格 -->
        <Border Grid.Row="3" CornerRadius="6" Background="{DynamicResource MahApps.Brushes.Control.Background}">
            <DataGrid ItemsSource="{Binding HistoryAlarms}" AutoGenerateColumns="False"
                      IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                      CanUserAddRows="False" CanUserDeleteRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding AlarmId}" Width="50" />
                    <DataGridTextColumn Header="报警名称" Binding="{Binding AlarmName}" Width="*" />
                    <DataGridTextColumn Header="级别" Binding="{Binding Level}" Width="80" />
                    <DataGridTextColumn Header="开始时间" Binding="{Binding StartTime, StringFormat=MM-dd HH:mm:ss}" Width="140" />
                    <DataGridTextColumn Header="结束时间" Binding="{Binding EndTime, StringFormat=MM-dd HH:mm:ss}" Width="140" />
                    <DataGridTextColumn Header="持续(秒)" Binding="{Binding DurationSec, StringFormat={}{0:F1}}" Width="80" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
