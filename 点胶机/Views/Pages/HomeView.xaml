<UserControl x:Class="点胶机.Views.Pages.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:点胶机.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:HomeViewModel}"
             mc:Ignorable="d">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Title}" FontSize="22" FontWeight="Bold" Margin="0,0,0,16" />

        <!-- OEE 看板 -->
        <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,16">
            <Border Margin="4" Padding="16" CornerRadius="6" Background="{DynamicResource MahApps.Brushes.Accent2}">
                <StackPanel>
                    <TextBlock Text="今日产量" Opacity="0.7" />
                    <TextBlock Text="{Binding TodayYield}" FontSize="32" FontWeight="Bold" />
                </StackPanel>
            </Border>
            <Border Margin="4" Padding="16" CornerRadius="6" Background="{DynamicResource MahApps.Brushes.Accent3}">
                <StackPanel>
                    <TextBlock Text="良率" Opacity="0.7" />
                    <TextBlock Text="{Binding OkRate}" FontSize="32" FontWeight="Bold" />
                </StackPanel>
            </Border>
            <Border Margin="4" Padding="16" CornerRadius="6" Background="{DynamicResource MahApps.Brushes.Accent4}">
                <StackPanel>
                    <TextBlock Text="运行时间" Opacity="0.7" />
                    <TextBlock Text="{Binding RunTime}" FontSize="20" FontWeight="Bold" VerticalAlignment="Bottom" Margin="0,12,0,0" />
                </StackPanel>
            </Border>
            <Border Margin="4" Padding="16" CornerRadius="6" Background="{DynamicResource MahApps.Brushes.Gray9}">
                <StackPanel>
                    <TextBlock Text="报警数" Opacity="0.7" />
                    <TextBlock Text="{Binding AlarmCount}" FontSize="32" FontWeight="Bold" Foreground="Orange" />
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- 三轴实时位置 -->
        <Border Grid.Row="2" Padding="16" CornerRadius="6" Background="{DynamicResource MahApps.Brushes.Control.Background}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="三轴实时位置" FontSize="16" FontWeight="Bold" Margin="0,0,0,12" />

                <Grid Grid.Row="1" Margin="0,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="40" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="80" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="X 轴" FontSize="16" VerticalAlignment="Center" />
                    <TextBlock Grid.Column="1" Text="{Binding PosX, StringFormat={}{0:F3} mm}" FontSize="16" VerticalAlignment="Center" Margin="16,0" />
                    <TextBlock Grid.Column="2" Text="{Binding XHomed, Converter={StaticResource BoolToText}}" VerticalAlignment="Center" />
                </Grid>
                <Grid Grid.Row="2" Margin="0,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="40" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="80" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Y 轴" FontSize="16" VerticalAlignment="Center" />
                    <TextBlock Grid.Column="1" Text="{Binding PosY, StringFormat={}{0:F3} mm}" FontSize="16" VerticalAlignment="Center" Margin="16,0" />
                    <TextBlock Grid.Column="2" Text="{Binding YHomed, Converter={StaticResource BoolToText}}" VerticalAlignment="Center" />
                </Grid>
                <Grid Grid.Row="3" Margin="0,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="40" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="80" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Z 轴" FontSize="16" VerticalAlignment="Center" />
                    <TextBlock Grid.Column="1" Text="{Binding PosZ, StringFormat={}{0:F3} mm}" FontSize="16" VerticalAlignment="Center" Margin="16,0" />
                    <TextBlock Grid.Column="2" Text="{Binding ZHomed, Converter={StaticResource BoolToText}}" VerticalAlignment="Center" />
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
