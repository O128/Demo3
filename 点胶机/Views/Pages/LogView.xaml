<UserControl x:Class="点胶机.Views.Pages.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:点胶机.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:LogViewModel}"
             mc:Ignorable="d">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Title}" FontSize="22" FontWeight="Bold" Margin="0,0,0,12" />

        <!-- 筛选条 -->
        <Border Grid.Row="1" Padding="12" CornerRadius="6" Background="{DynamicResource MahApps.Brushes.Control.Background}" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="最近 " VerticalAlignment="Center" />
                <ComboBox Width="80" SelectedIndex="0" VerticalAlignment="Center"
                          SelectionChanged="OnDaysChanged">
                    <ComboBoxItem Content="1 天" Tag="1" />
                    <ComboBoxItem Content="3 天" Tag="3" />
                    <ComboBoxItem Content="7 天" Tag="7" />
                </ComboBox>
                <TextBlock Text=" 天  级别:" VerticalAlignment="Center" Margin="16,0,0,0" />
                <ComboBox Width="120" Margin="8,0" VerticalAlignment="Center"
                          ItemsSource="{Binding LevelOptions}" SelectedItem="{Binding LevelFilter}" />
                <TextBlock Text="模块:" VerticalAlignment="Center" Margin="16,0,0,0" />
                <TextBox Width="100" Margin="8,0" Text="{Binding ModuleFilter, UpdateSourceTrigger=PropertyChanged}" />
                <Button Content="查询" Padding="14,4" Command="{Binding LoadLogsCommand}" Margin="16,0,0,0" />
            </StackPanel>
        </Border>

        <!-- 日志表格 -->
        <Border Grid.Row="2" CornerRadius="6" Background="{DynamicResource MahApps.Brushes.Control.Background}">
            <DataGrid ItemsSource="{Binding Logs}" AutoGenerateColumns="False"
                      IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                      CanUserAddRows="False" CanUserDeleteRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="时间" Binding="{Binding Timestamp, StringFormat=MM-dd HH:mm:ss.fff}" Width="150" />
                    <DataGridTextColumn Header="级别" Binding="{Binding Level}" Width="80" />
                    <DataGridTextColumn Header="模块" Binding="{Binding Module}" Width="100" />
                    <DataGridTextColumn Header="消息" Binding="{Binding Message}" Width="*" />
                    <DataGridTextColumn Header="机器" Binding="{Binding MachineName}" Width="100" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
